---
// Small pill. Usage: <Tag>rust</Tag>
---

<span
  style="display: inline-block; font-family: var(--font-mono); font-size: 0.72rem; font-weight: 500; letter-spacing: 0.05em; text-transform: uppercase; padding: 0.15rem 0.55rem; border: 1px solid var(--paper-edge); border-radius: var(--radius-pill); color: var(--ink-mute); background: var(--paper-deep);"
>
  <slot />
</span>
